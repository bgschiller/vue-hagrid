language: node_js
node_js: 8
script:
   - npm run test
   - cd demo && npm run build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: demo
  on:
    branch: master